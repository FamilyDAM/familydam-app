[feature name=familydam]
#    com.familydam.apps.photos.servlets.ImageThumbnailServlet
#        resize-library="image-magik"

[:repoinit]
    create service user familydam-admin

    create path (sling:Folder) /content
    create path (sling:Folder) /content/apps

    set ACL for familydam-admin
        allow   jcr:read,rep:write    on /
    end

    set ACL for everyone
        allow   jcr:read	on /content
        allow   jcr:read	on /content/apps
    end

    set ACL for anonymous
        allow   jcr:read	on /content/apps
    end

[variables]
    core.version=0.2.0-SNAPSHOT
    dashboard.version=0.2.0-SNAPSHOT
    photos.version=0.2.0-SNAPSHOT
    files.version=0.2.0-SNAPSHOT
    usermanager.version=0.2.0-SNAPSHOT
    sync.version=0.2.0-SNAPSHOT

[artifacts]
   com.familydam.osgi/osgi-bundle-core/${core.version}
   com.familydam.apps/osgi-bundle-dashboard/${dashboard.version}
#   com.familydam.apps/osgi-bundle-usermanager/${usermanager.version}
#   com.familydam.apps/osgi-bundle-sync/${sync.version}
#   com.familydam.apps/osgi-bundle-photos/${photos.version}
   com.familydam.apps/osgi-bundle-files/${files.version}
